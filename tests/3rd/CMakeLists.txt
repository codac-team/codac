# ==================================================================
#  tubex-lib / tests - cmake configuration file
# ==================================================================

  if(WITH_CAPD)
    add_subdirectory(ode)
  endif()